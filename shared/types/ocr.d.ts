declare module 'esearch-ocr' {
  // This is the basic declaration. You can expand this as needed.
  export function init(opts:any): any;
  // If you know the types, replace `any` with the correct type.
}